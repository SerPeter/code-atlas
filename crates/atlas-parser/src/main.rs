//! atlas-parser: Fast AST parsing for Code Atlas.
//!
//! Parses source files using tree-sitter and outputs structured JSON
//! representing code entities (functions, classes, imports, etc.)
//! for ingestion into the Code Atlas graph.

fn main() {
    eprintln!("atlas-parser: not yet implemented");
    std::process::exit(1);
}
