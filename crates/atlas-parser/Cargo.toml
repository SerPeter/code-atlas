[package]
name = "atlas-parser"
version = "0.1.0"
edition = "2024"
description = "Fast AST parser for Code Atlas"
license = "Apache-2.0"

[dependencies]
tree-sitter = "0.24"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
